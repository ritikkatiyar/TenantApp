server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: tenant-living-ai-service

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: ${HIKARI_MAX_POOL_SIZE:5}
      minimum-idle: ${HIKARI_MIN_IDLE:1}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        type:
          preferred_uuid_jdbc_type: CHAR
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  flyway:
    enabled: false

  ai:
    model:
      chat: ${SPRING_AI_MODEL_CHAT:google-genai}
    google:
      genai:
        api-key: ${GEMINI_API_KEY:AIzaSyAzjN-bhhGC98DrSWwyftvdWlVBspjscic}
        chat:
          options:
            model: ${GEMINI_MODEL:gemini-2.5-flash}
            temperature: ${GEMINI_TEMPERATURE:0.2}

app:
  jwt:
    secret: ${APP_JWT_SECRET:dev-only-change-this-to-a-long-random-secret-32b-min}
    access-expiration-ms: ${APP_JWT_ACCESS_MS:900000}
  ai:
    enabled: ${APP_AI_ENABLED:false}
  backend:
    client:
      base-url: ${BACKEND_BASE_URL:http://localhost:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    export:
      simple:
        enabled: true

logging:
  level:
    root: INFO
    com.tenantliving: DEBUG
